import { LoadingPage } from '@/components/common/LoadingPage';

/**
 * Loading page for furniture & fixtures inventory.
 * Displayed while inventory batches are being loaded from the server.
 */
export default function Loading() {
  return <LoadingPage />;
}
