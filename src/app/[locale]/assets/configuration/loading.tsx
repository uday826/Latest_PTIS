'use client';

import LoadingPage from '@/components/common/LoadingPage';

/**
 * Loading component for the Configuration Master module.
 */
export default function Loading() {
  return (
    <LoadingPage 
      translationNamespace="common.loading" 
    />
  );
}
